namespace Kcsar.Database.Web
{
  using Ninject.Modules;

  /// <summary>Defines dependency injection bindings for the TCA web site.</summary>
  public class DIModule : NinjectModule
  {
    /// <summary>Loads bindings for the TCA web site.</summary>
    public override void Load()
    {
  
    }
  }
}